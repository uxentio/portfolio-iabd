# ── Core ──
pydantic>=2.6
python-dotenv>=1.0

# ── LangChain 1.x ──
langchain>=0.3
langchain-core>=0.3
langchain-community>=0.3
langchain-chroma>=0.1.4
langchain-huggingface>=0.1.0
langchain-openai>=0.2

# ── LangGraph (StateGraph custom + checkpointing) ──
langgraph>=0.2.30
langgraph-checkpoint>=2.0
langgraph-checkpoint-sqlite>=2.0

# ── LLM: cliente OpenAI-compatible (apunta a LM Studio) ──
openai>=1.40

# ── RAG (embeddings locales + vectorstore) ──
chromadb>=0.5
sentence-transformers>=3.0

# ── RPA ──
playwright>=1.45

# ── Scraping ligero (consultar_libreria_externa) ──
requests>=2.31
beautifulsoup4>=4.12

# ── API (FastAPI con SSE, la consume el cliente MAUI) ──
fastapi>=0.115
uvicorn[standard]>=0.30
